# Architecture (Public)

This repository publishes sanitized automation components:
- market scanners
- orchestration wrappers
- latency monitoring helper

Private state (memory, logs, credentials) is intentionally excluded.
